# Config without an explicit type is deprecated.
mainmenu "mainmenu"

    config SELECTOR
        bool "selector"
        default y

    config BOARD_NAME
        default "board_a" if SELECTOR
        default "board_b"
