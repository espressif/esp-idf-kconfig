mainmenu "Set and set default in non-bool configs"

    config NONBOOL
        int "Non-bool config"
        default 0
        set A=1
        set default A=42

    config A
        int "A"
        default 0
